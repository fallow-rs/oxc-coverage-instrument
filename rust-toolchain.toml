[toolchain]
# Pinned so local builds use the same toolchain as CI. The MSRV remains 1.92
# (declared in workspace `Cargo.toml`) and is enforced by the dedicated MSRV
# CI job; this file only pins the lint / formatter floor.
channel = "1.95"
components = ["clippy", "rustfmt"]
